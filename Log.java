/*
 * Logging class to create logs to be stored on the system so that post crash 
 * reports can be seen.
 */
public class Log {
	boolean ENABLE = true;
	public Log(){
		//initialize log file. Append if existing, create if not.
		
	}
}
